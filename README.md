# Luflex - Infraestructura Base de Comunicación (Docker IoT)

Este repositorio contiene la configuración empaquetada en Docker para desplegar la infraestructura de red, bróker MQTT y el panel de control (Dashboard) del proyecto **Luflex** (Sistema Automático de Cortinas Inteligentes).

---

## 🛠️ Requisitos Previos
Asegúrese de tener instalado en su sistema:
* **Docker Desktop** (Windows / macOS) o **Docker & Docker Compose** (Linux / Raspberry Pi OS).

---

## 🚀 Pasos para Inicializar el Entorno

### 1. Clonar el repositorio
```bash
git clone <URL_DE_TU_REPOSITORIO>
cd luflex-docker